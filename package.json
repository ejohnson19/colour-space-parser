{
  "name": "colour-space-parser",
  "version": "0.1.0",
  "description": "Validating parser and pretty printer for CSS colour notation (hex, rgb, hsl, oklch).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "pretest": "tsc -p tsconfig.test.json",
    "test": "node --test dist-test/test"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
